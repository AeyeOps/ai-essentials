[project]
name = "ai-essentials"
version = "0.0.10"
description = "AI development utilities, scripts, and services"
readme = "README.md"
license = "MIT"
requires-python = ">=3.12.3"

[tool.uv]
managed = true

[tool.uv.workspace]
members = ["packages/*"]
