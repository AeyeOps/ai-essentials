# AEO Ghostty session launcher — shared settings.
#
# Included from the main Ghostty config via `config-file = aeo-launcher.conf`.
# Ghostty resolves the relative path against ~/.config/ghostty/, and an included
# file's keys override the parent's on conflict — so these settings win even if
# the parent config set them. The installer discloses the keys below before it
# adds the include line.

# Native splits stay native: each new surface runs the rc-guard launcher and
# lands in its own tmux session, so splits are recoverable per-surface.
keybind = alt+d=new_split:right
keybind = alt+shift+d=new_split:down

# Tiling/CSD mouse-offset fix (gated — uncommented by the installer ONLY when a
# tiling WM is detected). Under a tiling WM (e.g. KDE/KWin) Ghostty's GTK
# client-side-decoration shadow margins (_GTK_FRAME_EXTENTS) desync the
# mouse->cell mapping by several columns, because the WM places the window by its
# frame, not its content box. `window-decoration = none` zeroes the extents so the
# window box equals the content box and selection maps 1:1. Diagnose with
# `xprop _GTK_FRAME_EXTENTS` on the Ghostty window (nonzero left = the offset).
# Do NOT enable globally — it also removes the titlebar on non-tiling desktops.
# window-decoration = none
